# Literature Digests

Place per-paper digests here as `*.mdx` with front matter (`title`, `slug`/bibkey, optional `tags`, `internal: true` for non-public notes). Follow the template in `packages/thesis/docs/workflows.md` (see "Literature digest recipe").
