[package]
name = "billiard"
version = "0.1.0"
edition = "2021"
description = "Minkowski billiard algorithm for Lagrangian product capacity"

[dependencies]
geom = { path = "../geom" }

[dev-dependencies]
