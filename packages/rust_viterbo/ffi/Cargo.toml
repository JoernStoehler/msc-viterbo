[package]
name = "rust_viterbo_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rust_viterbo_geom = { path = "../geom" }
pyo3 = { version = "0.22", features = ["extension-module"] }
