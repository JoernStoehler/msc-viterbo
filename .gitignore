# Root-level ignores that apply across packages. Package-local build outputs stay in packages/*/.gitignore files.
# When adding a new toolchain, prefer updating the relevant package-level ignore file unless artifacts appear repo-wide.

# Devcontainer + developer-local secrets
.devcontainer/.env
.env
.env.*
!.env.example

# Claude Code local settings (gitignored by design)
.claude/settings.local.json
.claude/CLAUDE.local.md
# Editor/IDE state that should never be shared
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.code-workspace

# OS-level metadata
.DS_Store
Thumbs.db

# Python: caches, envs, coverage, packaging detritus (experiments + notebooks in thesis)
**/__pycache__/
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/.ipynb_checkpoints/
**/.venv/
.venv/
*.py[cod]
*.pyo
.coverage
.coverage.*
htmlcov/
.eggs/
*.egg-info/
pip-wheel-metadata/

# Data outputs (generated by experiments, can be regenerated)
data/outputs/

# Rust/Cargo artefacts (rust_viterbo and any helper crates)
target/
**/target/

# Node/Astro/Vite/etc. build outputs that can show up in docs-site or future packages
**/node_modules/
**/.astro/
**/dist/
**/.turbo/
**/.parcel-cache/
**/.next/
**/coverage/

# Lean4 Lake build outputs (lean_viterbo) and generated binaries
**/.lake/
*.olean
*.olean.trace

# Generic backups + temp files created during editing/docs builds
*.log
*.tmp
*.bak.*
*.swp
*.swo
log/
tmp/

# Local git worktree directories should live outside the repo root. Ignore in case someone creates one here accidentally.
worktrees/
scratch/
