# Shared Cargo target dir across all worktrees to avoid rebuilding artifacts.
# In local devcontainer, CARGO_TARGET_DIR env var points to /workspaces/worktrees/shared/target.
# In web environment, uses default target/ directory (no worktrees).
[build]
# target-dir intentionally not set here - use CARGO_TARGET_DIR env var in local devcontainer
